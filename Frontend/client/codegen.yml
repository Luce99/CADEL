overwrite: true
schema: "http://localhost:5010/graphql"
documents: "src/**/*.graphql"
generates:
  http://localhost:3000/:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
      - "fragment-matcher"
